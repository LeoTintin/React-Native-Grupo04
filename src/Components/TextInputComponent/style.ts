import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  input: {
    alignItems: 'center',
    width: "95%",
    height: 60,
    backgroundColor: "#fff",
    borderRadius: 50,
    borderColor: "#ccc",
    borderWidth: 1,
    marginBottom: 10,
    paddingHorizontal: 10,
    paddingLeft: 20,
    fontSize: 15
  },
});
